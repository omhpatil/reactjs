function App() {
  return (
    <h1>Learning React JS</h1>
  );
}

export default App;


// here we simply writing code inside one function which basically return a html tags and contenet inside it 
// and then we just export this js file and inside the index.js we import it 